apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "myhpa.fullname" . }}-test-connection"
  labels:
{{ include "myhpa.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "myhpa.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
